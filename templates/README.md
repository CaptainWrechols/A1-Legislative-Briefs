# Templates

- `citizen-brief/front-brief-shell.md` — optional section checklist for the 1–2 page citizen handout (module-mapped to the Phase 2 visual system)
- `phase-2-samples/` — official Phase 2 Issue Brief sample; the authoritative source for colors/fonts/spacing/modules (never its text)

Agent instructions live in `agents/`. Brand tokens live in `config/forum-brand.yaml`.
